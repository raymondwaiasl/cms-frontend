import { Dialog, Drawer } from '@mui/material';

const DialogWrapper = (type: string) => {
  if (type) {
  }
};

export default DialogWrapper;

export type DialogWrapperProps = {
  type: 'DIALOG' | 'SIDEPANEL';
};
